Um den Server zu starten muss die manage.py-Datei in der 
Konsole mit dem Befehl "python manage.py runserver 0.0.0.0:8000" ausgeführt werden.
Der Client kann im Browser über die IP-Adresse des Servers auf port 8000 erreicht werden.